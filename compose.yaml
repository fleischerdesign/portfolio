services:
  portfolio:
    build: app
    labels:
      caddy: fleischer.design
      caddy.reverse_proxy: "{{upstreams 3000}}"
    env_file: .env
    restart: unless-stopped
    networks:
      - web
    volumes:
      - ./data:/app/data
networks:
  web:
    external: true